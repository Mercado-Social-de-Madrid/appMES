meta {
  name: Logout User
  type: http
  seq: 2
}

get {
  url: {{URL}}/api/logout/
  body: none
  auth: none
}

headers {
  Authorization: Token {{token}}
}

auth:bearer {
  token: Token {{token}}
}

script:pre-request {
  
  
}
